import React, { Component, Fragment } from 'react';

class Auth extends Component {
    constructor (props) {
        super(props);
        this.state = {
        }
    }

    render () {
        return (
            <section className="login">

            </section>
        )
    }
}

export default Auth;